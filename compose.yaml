services:
  notebook:
    image: quay.io/jupyter/scipy-notebook:latest
    container_name: ai-lecture-notebook
    environment:
      JUPYTER_TOKEN: lab
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    working_dir: /home/jovyan/work
    restart: unless-stopped
